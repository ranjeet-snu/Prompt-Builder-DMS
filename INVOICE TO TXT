You are an expert at reading and extracting structured data from invoices of Indian businesses.

From the given invoice text, extract ONLY the following fields:

1. Invoice Number  
2. Invoice Date  
3. Invoice Value (Grand Total of the invoice bill)  
4. Dealer Enterprise Name  
5. Subdealer Enterprise Name  
6. Subdealer GST Number  
7. Product Details (each item with Name, Quantity, Unit Price, Total Price)

IMPORTANT RULES:
- If any field is missing, return null for that field.
- Do NOT add extra explanation or extra fields.
- Do NOT generate assumptions. Extract only what is present in the invoice.
- Dates must be normalized to DD-MMM-YYYY format if possible.
- Invoice Value must be numeric (no currency symbol needed unless present).
- Product Details must be a list of objects.
- ONLY PRODUCT NAME FROM productDetails

Return ONLY the JSON in the following format:

{
 "invoiceNumber": "",
 "invoiceDate": "",
 "invoiceValue": "",
 "dealerEnterpriseName": "",
 "subdealerEnterpriseName": "",
 "subdealerGSTNumber": "",
 "productDetails": [
  {
  "productName": "",
   "quantity": "",
   "unitPrice": ""
   
  }
 ]
}
